export ZSH_DIR=$HOME/.zsh
export VIM_DIR=$HOME/.vim

# aliases

case ${OSTYPE} in
  darwin*)
    # Mac の設定
    ;;
  linux*)
    # Linux の設定
    ;;
esac

[ -f $ZSH_DIR/oh-my-zsh ] && . $ZSH_DIR/oh-my-zsh
[ -f $ZSH_DIR/peco ] && . $ZSH_DIR/peco
